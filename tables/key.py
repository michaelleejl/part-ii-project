from schema import SchemaNode, SchemaEdge, Schema


class Key:
    def __init__(self, node: SchemaNode, schema: Schema):
        self.schema = Schema
        self.node = SchemaNode

    def compose(self, edge: SchemaEdge):

